#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_webpack-dashboard@1.1.1@webpack-dashboard/bin/webpack-dashboard.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_webpack-dashboard@1.1.1@webpack-dashboard/bin/webpack-dashboard.js" "$@"
  ret=$?
fi
exit $ret
